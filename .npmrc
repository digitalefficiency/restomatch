strict-peer-dependencies=false
auto-install-peers=true

# pnpm + Expo/React Native compat: hoist all transitive deps to the
# workspace root so Metro's CommonJS resolver can find them. Without
# this, RN's runtime imports (invariant, @babel/runtime, hermes-parser,
# etc.) aren't reachable from apps/mobile.
shamefully-hoist=true
